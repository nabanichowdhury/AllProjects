const mynumber=-5;
const absolute=Math.abs(mynumber)
console.log(absolute);

// let p=Math.random()*100;
// let r=Math.round(p);


for(let i=0;i<=30;i++){
    let p=Math.random()*100;
let r=Math.round(p);
    console.log(r);
}
